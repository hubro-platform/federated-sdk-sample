# Example of a Federated Job Created Using Hubro SDK

## Build

```Bash
export PATH=/opt/homebrew/Cellar/llvm/18.1.8/bin/:$PATH
cargo build --target wasm32-wasip1 --release  
```

Artifact is located at ./target/wasm32-wasip1/release/federated_sample.wasm
